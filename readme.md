# Project 9 - SAT Solvers

> Team Metric TSPMO

## Running the Benchmarks

> The project uses [`uv`](https://docs.astral.sh/uv) to manage the Python version and dependencies.

To install the dependencies and run the tests:

```
uv sync
make test
```

To generate the report and charts:

```
make report
make charts
```

